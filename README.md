
  # E-Mow Lawncare Website

  This is a code bundle for E-Mow Lawncare Website. The original project is available on request

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  
